# Some Debian / RasPiOS bulk useful commands

```commandline
$ route  
$ iwlist wlan0 scan
$ iwlist --help  
$ netstat -tunap
$ ifconfig
$ iwconfig
$ sudo mntui
$ sudo raspi-config
$ wpa_cli
  > scan
  > scan results
  > add-network
     4
  > set_network 4 ssid "RPiNet"
  > set_network 4 psk "PassWord"
  > quit
$ htop  
$ lsb_release -a
$ tree
$ tree -hpu

... more to come
```

--- 