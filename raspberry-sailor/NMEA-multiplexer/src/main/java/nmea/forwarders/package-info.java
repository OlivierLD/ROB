/**
 * Contains the servers used for re-broadcasting.
 *
 * Servers, aka forwarders, suppliers, etc.
 * Make sure all Beans subclass in Forwarders have getters,
 * like for "public static class TCPBean { ... }"
 */
package nmea.forwarders;
