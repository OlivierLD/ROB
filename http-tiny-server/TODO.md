# Ideas, etc...

Take a look at the programming interface at [Javalin](https://javalin.io/)...  
It's interesting.

---

- Have a default `RESTRequestManager`, that will manage the `GET` requests for static documents,
like the system properties `static.docs` and `static.zip.docs`.
- Features like `jsp` amd `asp` ?

---